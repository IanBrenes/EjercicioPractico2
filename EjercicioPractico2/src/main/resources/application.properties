# --- APP ---
spring.application.name=EjercicioPractico2
server.port=87
spring.main.banner-mode=off
spring.thymeleaf.cache=false

# --- DATASOURCE (MySQL 8) ---
spring.datasource.url=jdbc:mysql://localhost:3306/CineTeatro?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=usuario_prueba
spring.datasource.password=Usuario_Clave.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / HIBERNATE ---
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
# (opcional, por si acaso) fuerza también por propiedad hibernate:
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# --- LOGGING útil ---
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# --- MAIL (no afecta el arranque si no envías correos) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=brenes.ian.cr7@gmail.com
spring.mail.password=vkpmrjgxzntqwhyl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- Misc ---
servidor.http=localhost
